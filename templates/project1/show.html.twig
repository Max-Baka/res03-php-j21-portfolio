{% extends 'base.html.twig' %}

{% block title %}Project1{% endblock %}

{% block body %}
    <h1>Project1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project1.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project1.Name }}</td>
            </tr>
            <tr>
                <th>Firsttechno</th>
                <td>{{ project1.Firsttechno }}</td>
            </tr>
            <tr>
                <th>Secondtechno</th>
                <td>{{ project1.Secondtechno }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project1.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_project1_index') }}">back to list</a>

    <a href="{{ path('app_project1_edit', {'id': project1.id}) }}">edit</a>

    {{ include('project1/_delete_form.html.twig') }}
{% endblock %}
